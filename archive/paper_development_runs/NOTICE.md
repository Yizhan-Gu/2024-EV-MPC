# Archived paper development runs

This directory preserves superseded intermediate outputs for provenance only.
They were generated before the final sparse charger formulation,
lexicographic tie-break, and Q3 protocol were frozen. They must not be cited or
mixed with the audited outputs under `experiments/results/paper_2023Q3/`.

The archived CSV files remain local and are ignored by Git.
